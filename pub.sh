# git init # Initialize git repository
# export PATH=/usr/local/share/npm/bin:$PATH    # Add npm to PATH
# git checkout -b my-new-branch # Create new branch
# git remote -v # Check remote repository
# git remote add origin https://github.com/jookie/jojoStock.git  # Add remote repository
git push --set-upstream origin main
git branch -M main
git add .
git commit -m "0.0.1 added files" 
git push -u origin main

# rm -rf .git # Remove git repository
# pip2 install -r requirements.txt # Install dependencies

# python -m venv .venv  # Create virtual environment
# source .venv/bin/activate  # Activate virtual environment
# python3 -m streamlit run app.py # Run Streamlit app
  
### How to run it on your own machine

# 1. Install the requirements

#    ```
#    $ pip install -r requirements.txt
#    ```

# 2. Run the app

#    ```
#    $ streamlit run streamlit_app.py
#    ```
